[project]
name = "mazegen"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = ["flake8", "mypy"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["mazegen", "mazegen_bonus"]